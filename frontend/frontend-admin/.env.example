# Backend API origin (optional in dev — Vite proxy handles /api and /auth when empty).
# Use http://localhost:3000 or http://localhost:3000/api — both work (trailing /api is stripped).
# VITE_API_BASE_URL=http://localhost:3000
