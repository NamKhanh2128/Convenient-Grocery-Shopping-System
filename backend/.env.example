PORT=
JWT_SECRET=your-jwt-secret

# Supabase — Direct connection (port 5432)
# Thay [YOUR-PASSWORD] bằng mật khẩu database (Project Settings → Database)
# Nếu mật khẩu có ký tự đặc biệt (@, #, ...) cần URL-encode trong chuỗi URI
DATABASE_URL=
DB_POOL_MAX=