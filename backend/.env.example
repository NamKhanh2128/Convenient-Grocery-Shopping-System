PORT=3000
JWT_SECRET=your-jwt-secret

# --- Khuyến nghị (Windows / mạng không có IPv6): Transaction pooler port 6543 ---
# Supabase → Project Settings → Database → Connection string → URI → Transaction pooler → Copy
# User dạng: postgres.vzxmdvesbbjaubgasihb (KHÔNG chỉ "postgres")
DATABASE_URL=postgresql://postgres.vzxmdvesbbjaubgasihb:[YOUR-PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# --- Direct (port 5432) — host db.xxx chỉ có IPv6, dễ lỗi ENOTFOUND trên một số mạng ---
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.vzxmdvesbbjaubgasihb.supabase.co:5432/postgres

DB_POOL_MAX=10
